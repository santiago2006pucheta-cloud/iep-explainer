# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your-key-here
# Optional: override the port (default 5000)
# PORT=5000
# Optional: set to 1 to enable Flask's debug mode while developing (off by default)
# FLASK_DEBUG=1
